name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: |
          echo "Hello World!"
          echo "Or whoever is there..."
      - name: Print Goodbye
        run: echo "Im Done, Bya Bye!"
